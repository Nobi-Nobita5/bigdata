import org.apache.spark.sql.SparkSession
import org.apache.spark.sql.functions._
object a表结构和DataFrame {
  def main(args: Array[String]): Unit = {
    val rows = Seq(("Matei Zhaharia","CA"),("Jas Maz","CA"))
    val authorsDF = row
  }
}
